<header class="main-header">
	<div class="logo-container">
		<div class="logo">
			<a href="{{ path('app_home') }}">EYSA BOUCHERIE</a>
		</div>
	</div>

	<div class="header-container">
		<nav class="nav-links">
			<ul>
				<li>
					<a href="{{ path('app_home') }}">ACCUEIL</a>
				</li>

				{# 🔽 MOUNT POINT VUE 3 ICI - POUR DropdownMenu.vue #}
				<li id="vue-dropdown-menu" {% if categoriesJson is defined %} 
				data-categories='{{ categoriesJson|json_encode|e("html_attr") }}' {% endif %}>
					<a href="#">Nos produits</a>
				</li>


				<li>
					<a href="{{ path('app_click_collect') }}">CLICK & COLLECT</a>
				</li>
				<li>
					<a href="{{ path('app_philosophy') }}">NOTRE PHILOSOPHIE</a>
				</li>
				<li>
					<a href="{{ path('app_contact') }}">CONTACT</a>
				</li>
			</ul>
		</nav>

		<div class="header-icons">
			{# Gestion intelligente du lien "Mon Compte" #}
			{% if is_granted('ROLE_USER') %}
				{# Utilisateur connecté - Lien vers son compte #}
				<a href="{{ path('app_account') }}" class="icon-link">
					<i class="fa-solid fa-user" aria-hidden="true"></i>
					<span class="label">MON COMPTE</span>
				</a>
			{% else %}
				{# Utilisateur pas connecté - Lien vers la connexion #}
				<a href="{{ path('app_login') }}" class="icon-link">
					<i class="fa-solid fa-user" aria-hidden="true"></i>
					<span class="label">CONNEXION</span>
				</a>
			{% endif %}

			<a href="{{ path('app_cart_index') }}" class="icon-link cart-icon-container" aria-label="Panier" data-tooltip="Voir mon panier">
				<i class="fa-solid fa-cart-shopping cart-icon" aria-hidden="true"></i>
				<span id="cart-badge" data-count="0" aria-label="0 articles dans le panier">0</span>
			</a>

			<!-- searchBar monté par Vue -->
			<div class="search-container"></div>
		
			{# <div class="search-container">
				<a href="#" class="icon-link" id="searchToggle" aria-label="Rechercher">
					<i class="fa-solid fa-magnifying-glass" aria-hidden="true"></i>
				</a>
				<div class="search-dropdown" id="searchDropdown">
					<input type="text" id="searchInput" placeholder="Rechercher un produit..." autocomplete="off">
					<div id="searchResults">
						<p class="search-hint">Tapez au moins 2 caractères...</p>
					</div>
				</div>
			</div> #}
		</div>
	</div>

	{# ASSETS ENCORE : À PLACER EN BAS DU HEADER #}
	{{ encore_entry_link_tags('app') }}
	{{ encore_entry_script_tags('app') }}
</header>
