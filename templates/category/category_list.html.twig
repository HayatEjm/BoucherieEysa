{% extends 'base.html.twig' %}

{% block title %}Nos catégories{% endblock %}

{% block body %}
    <section class="categories-container">
        <h1 class="section-title">Découvrez nos catégories</h1>
        <p class="section-description">
            Retrouvez ici toutes nos catégories de viandes et produits. Cliquez sur l'une d'elles pour découvrir les produits disponibles.
        </p>

        <div class="categories-grid">
            {% for category in categories %}
                <div class="category-card"
                     style="background-image: url('{% if category.image %}{{ asset('images/' ~ category.image) }}{% else %}{{ asset('images/fallback.jpg') }}{% endif %}');">

                    <div class="category-overlay">
                        <h2 class="category-name">{{ category.name }}</h2>

                        {% if category.description %}
                            <p class="category-description">{{ category.description }}</p>
                        {% endif %}

                        <a href="{{ path('app_category_show', { id: category.id }) }}" class="category-link">
                            Voir les produits
                        </a>
                    </div>
                </div>
            {% else %}
                <p>Aucune catégorie trouvée.</p>
            {% endfor %}
        </div>
    </section>
{% endblock %}
