{#
    Les styles CSS spécifiques à la page liste produits (ex : .products-grid, .product-card, etc.)
    sont désormais déplacés dans le fichier assets/styles/product/product_list.css,
    importé via assets/app.js.
    
    Cette séparation garantit :
    - Un template Twig allégé et lisible
    - Une centralisation des styles pour la maintenance
    - Le respect du MVC et des bonnes pratiques Symfony
#}
{% extends 'base.html.twig' %}

{% block title %}
    {% if page_title is defined %}
        {{ page_title }} - EYSA BOUCHERIE
    {% else %}
        Nos Produits - EYSA BOUCHERIE
    {% endif %}
{% endblock %}

{% block body %}
<div class="products-container">
    <div class="page-header">
        {% if category is defined %}
            <div class="breadcrumb">
                <a href="{{ path('app_category_index') }}">Catégories</a> 
                <span class="separator">></span> 
                <span class="current">{{ category.name }}</span>
            </div>
        {% endif %}
        
        <h1>
            {% if page_title is defined %}
                {{ page_title }}
            {% else %}
                Nos Produits
            {% endif %}
        </h1>
        
        <p class="subtitle">
            {% if page_subtitle is defined %}
                {{ page_subtitle }}
            {% else %}
                Découvrez notre sélection de viandes de qualité
            {% endif %}
        </p>
        
        {% if category is defined %}
            <a href="{{ path('app_products') }}" class="btn-eysa btn-eysa-outline">
                <i class="fa-solid fa-arrow-left" aria-hidden="true"></i> Voir tous les produits
            </a>
        {% endif %}
    </div>

    {% if products is not empty %}
        <div class="products-grid">
            {% for product in products %}
                <div class="product-card">
                    <div class="product-image">
                        {% if product.image %}
                            <img src="{{ asset('images/' ~ product.image) }}" alt="{{ product.name }}">
                        {% else %}
                            <img src="{{ asset('images/fallback.jpg') }}" alt="{{ product.name }}">
                        {% endif %}
                    </div>
                    
                    <div class="product-info">
                        <h3 class="product-name">{{ product.name }}</h3>
                        <p class="product-description">{{ product.description }}</p>
                        
                        <div class="product-price">
                            <span class="price">{{ product.price|number_format(2, ',', ' ') }}€</span>
                            <span class="unit">/ {{ product.unit }}</span>
                        </div>
                        
                        <div class="product-actions">
                            <a href="{{ path('app_product_show', {'id': product.id}) }}" class="btn-eysa btn-eysa-primary">
                                <i class="fa-solid fa-eye" aria-hidden="true"></i> Voir détails & Commander
                            </a>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% else %}
        <div class="no-products">
            <i class="fa-solid fa-box-open" aria-hidden="true"></i>
            <h3>Aucun produit trouvé</h3>
            <p>Il n'y a pas de produits dans cette catégorie pour le moment.</p>
        </div>
    {% endif %}
</div>
{% endblock %}
